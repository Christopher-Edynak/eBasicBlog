- @posts.each do |post|
  .post_wrapper
    %h2.title= link_to post.title, post
    %p.date
      %p.body= truncate(post.body, length:100)
      %br/
      = time_ago_in_words(post.created_at)
      ago